/* Create header content and main*/
/Figma

/html
1. Создать главный header с классом header.
2. [В header] создать контейнер с классом wrapper.
3. [В wrapper] создать контейнер header__wrapper.
4. [В header__wrapper] создать контейнер, внутри создать контейнер header__logo ,
5. [В header__logo] создать ссылку header__logo-link,при клике путь /, в alt передать текст с макета который на глв стрн.

6. [В header__wrapper] создать навиг контейнер в нём создать список ul_li*4 с такими классами header + nav & list & item
7. [В nav item] создать ссылку путь #! класс header__link
|||||||||
1. Создать main тег внутри стартовую секцию с классом intro


/css
1. [Для body] {
  стиль шрифта lato;
  стиль шрифта нормал;
  размер шрифта 16px;
  отступы шрифта 1.2;
  ширина шрифта нормал;
  цвет #102746;
}
2. [Для wrapper] {
  макс ширину 1060 px;
  центрировать по ширине блока;
}
3. [Для header] {
  Фиксируем вверху экрана
  задать позицию фиксированную;
  задать позицию вверх и лева к началу;
  ширина на всю ширину родительского блока;
}
4. [Для header__wrapper] {
  создать флекс контейнер;
  для дочерних элементов разреш упаковываться,на следующей строке;
  вертикальное выравнивание в конце;
  горизонтальное выравнивание по краям блока;
  добавить отступ внутренний с верху взять размер с макета;
}
5. [Для header__list] {
  создать флекс контейнер;
  для дочерних элементов разреш упаковываться,на следующей строке;
}
6. [Для header_item] {
  /Figma посмотреть отступ между ссылками в макета в блоке навигация.
  задать внешние отступ с лева на право в px который в макете задан
}
6.1 [Для header__item]:последнего элемента {
  обнулить отступы который остался"смотреть в консоле разработчика";
}
7. [Для header__link] {
  перенести с макета такие параметры ссылки: размер текста, растояние между текстом & цвет белый;
  Убрать нижнее подчёркивание;
}
8. [Для header__link]:при наведение или при фокусе или при клике на ссылку {
  добавить степень прозрачности 75
}
9. [Для intro] {
  задать временную высоту для корректного отображения картинки, 100 проц экрана;
   "% задаётся размет родительского блока"
  добавить для фона картинку взять её из фигмы в jpg: убрать повтор картинки при не полном заполнение пространнства,
и по центру;
  размер фона: покрывать всю ширину экрана;
}

/*Добавить в css комменты начало стилизации определенной секции и конец*/